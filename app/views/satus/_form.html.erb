<%= form_with(model: satu) do |form| %>
  <% if satu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(satu.errors.count, "error") %> prohibited this satu from being saved:</h2>

      <ul>
        <% satu.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post %>
    <%= form.text_area :post %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
